# 📂 玩转 Claude Code 系列教程 — 关键文件夹完全指南

> **一句话概括**：本篇专门介绍 Claude Code 安装后所有关键文件夹的结构、作用和管理方法，帮你彻底理解目录组织方式。

---

## 一、总述

Claude Code 安装后会在多个位置创建文件夹，理解这些 **目录结构** 是高效使用 Claude Code 的基础。本文按 **作用域** 组织，从系统级到项目级，逐层解析每个文件夹。

---

## 二、文件夹全景图

```
┌─────────────────────────────────────────────────────────────────┐
│                         系统级 (System)                          │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ macOS:   /Library/Application Support/ClaudeCode/          │ │
│  │ Linux:   /etc/claude-code/                                  │ │
│  │ Windows: C:\Program Files\ClaudeCode\                       │ │
│  └────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                         用户级 (User)                           │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │                    ~/.claude/                              │ │
│  │  ├── settings.json                  # 全局设置             │ │
│  │  ├── CLAUDE.md                      # 全局记忆             │ │
│  │  ├── skills/                        # 个人技能             │ │
│  │  ├── agents/                        # 子代理配置           │ │
│  │  └── projects/                      # 项目数据             │ │
│  └────────────────────────────────────────────────────────────┘ │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │                 ~/.claude.json                             │ │
│  │                 # 主配置文件（OAuth、MCP、主题）           │ │
│  └────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                       项目级 (Project)                           │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │                    your-project/                           │ │
│  │  ├── .claude/                       # 项目配置目录         │ │
│  │  │   ├── settings.json              # 项目设置             │ │
│  │  │   ├── settings.local.json        # 本地覆盖             │ │
│  │  │   ├── CLAUDE.md                  # 项目记忆             │ │
│  │  │   ├── CLAUDE.local.md            # 本地记忆             │ │
│  │  │   ├── skills/                    # 项目技能             │ │
│  │  │   ├── agents/                    # 项目代理             │ │
│  │  │   └── rules/                     # 模块化规则           │ │
│  │  └── .mcp.json                      # MCP 配置             │ │
│  └────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
```

---

## 三、系统级文件夹

### macOS

```
/Library/Application Support/ClaudeCode/
└── managed-settings.json    # IT/管理员部署的策略
```

**访问权限**：需要管理员权限（`sudo`）

**用途**：企业环境统一部署配置策略

### Linux/WSL

```
/etc/claude-code/
└── managed-settings.json    # IT/管理员部署的策略
```

**访问权限**：需要 root 权限（`sudo`）

### Windows

```
C:\Program Files\ClaudeCode\
└── managed-settings.json    # IT/管理员部署的策略
```

**访问权限**：需要管理员权限

---

## 四、用户级核心文件夹 `~/.claude/`

这是 Claude Code 最重要的配置目录，存储所有个人全局设置。

### 目录结构详解

```
~/.claude/
├── settings.json              # 全局设置文件
├── CLAUDE.md                  # 全局记忆文件
├── skills/                    # 个人技能目录
│   ├── explain-code/
│   │   └── SKILL.md
│   ├── deploy/
│   │   └── SKILL.md
│   └── ...
├── agents/                    # 子代理配置目录
│   ├── security-reviewer.md
│   ├── code-explorer.md
│   └── ...
├── commands/                  # 自定义命令（已合并到 skills）
│   └── ...
└── projects/                  # 项目数据目录
    ├── abc123def456/          # 项目 hash
    │   ├── memory/            # 自动记忆
    │   │   ├── MEMORY.md
    │   │   ├── debugging.md
    │   │   ├── api-conventions.md
    │   │   └── ...
    │   └── sessions/          # 会话历史
    │       ├── sess-001/
    │       ├── sess-002/
    │       └── ...
    └── ...
```

---

## 五、`~/.claude/skills/` — 技能文件夹

### 作用

存储 **个人技能**，这些技能在 **所有项目** 中都可用。

### 目录结构

```
~/.claude/skills/
├── explain-code/
│   └── SKILL.md               # 必需：技能定义
├── deploy-staging/
│   ├── SKILL.md               # 必需
│   ├── template.md            # 可选：模板
│   ├── examples/              # 可选：示例
│   │   └── successful-deploy.md
│   └── scripts/               # 可选：脚本
│       └── deploy.sh
├── fix-issue/
│   └── SKILL.md
└── code-review/
    └── SKILL.md
```

### 技能目录规范

| 元素 | 必需 | 说明 |
|------|------|------|
| `SKILL.md` | ✅ 是 | 技能定义文件（YAML frontmatter + Markdown） |
| `template.md` | ❌ 否 | Claude 填写的模板 |
| `examples/` | ❌ 否 | 示例输出，展示预期格式 |
| `scripts/` | ❌ 否 | Claude 可执行的脚本 |

### 技能命名规则

- 只能使用小写字母、数字和连字符
- 最大长度 64 字符
- 目录名 = `/` 命令名

### 创建技能

```bash
# 创建新技能目录
mkdir -p ~/.claude/skills/my-skill

# 创建技能文件
cat > ~/.claude/skills/my-skill/SKILL.md << 'EOF'
---
name: my-skill
description: 我的自定义技能
---

这里是技能的详细说明...
EOF
```

---

## 六、`~/.claude/agents/` — 子代理文件夹

### 作用

存储 **自定义子代理** 配置，用于专门的任务类型。

### 目录结构

```
~/.claude/agents/
├── security-reviewer.md       # 安全审查代理
├── code-explorer.md           # 代码探索代理
├── api-designer.md            # API 设计代理
├── test-writer.md             # 测试编写代理
└── performance-analyst.md     # 性能分析代理
```

### 子代理文件格式

每个子代理是一个独立的 Markdown 文件：

```markdown
---
name: security-reviewer
description: 审查代码中的安全漏洞
model: opus
tools: Read, Grep, Glob, Bash
---

你是一名高级安全工程师。审查代码时关注...

## 检查清单

1. 注入漏洞
2. 认证问题
3. 敏感数据暴露
4. 配置错误
```

---

## 七、`~/.claude/projects/` — 项目数据文件夹

### 作用

自动存储每个项目的 **记忆** 和 **会话历史**。

### 目录结构

```
~/.claude/projects/
└── <project-hash>/            # 基于项目路径生成的 hash
    ├── memory/                # 自动记忆
    │   ├── MEMORY.md          # 记忆索引（每次会话加载）
    │   ├── debugging.md       # 调试笔记
    │   ├── api-conventions.md # API 约定
    │   ├── architecture.md    # 架构笔记
    │   └── ...
    └── sessions/              # 会话历史
        ├── <session-id>/      # 每个会话一个目录
        │   └── transcript.jsonl
        └── ...
```

### 项目 Hash 生成

项目 hash 基于 **项目路径** 生成：

```
/Users/user/dev/my-project     →  abc123def456
/Users/user/dev/another-project → 789xyz012abc
```

### 自动记忆内容

| 文件 | 内容 |
|------|------|
| `MEMORY.md` | 所有记忆的索引，会话开始时加载 |
| `debugging.md` | 调试过程中发现的问题和解决方案 |
| `api-conventions.md` | API 设计决策和约定 |
| `architecture.md` | 架构洞察和关键组件说明 |

### 会话历史

每个会话保存为 `transcript.jsonl`，包含：
- 用户消息
- Claude 响应
- 工具调用
- 文件编辑快照

---

## 八、项目级文件夹 `.claude/`

### 作用

存储 **项目特定** 的配置和资源，可以提交到 Git 与团队共享。

### 目录结构

```
your-project/
└── .claude/
    ├── settings.json           # 项目设置（团队共享）
    ├── settings.local.json     # 本地覆盖（不提交）
    ├── CLAUDE.md               # 项目记忆（团队共享）
    ├── CLAUDE.local.md         # 本地记忆（不提交）
    ├── skills/                 # 项目技能
    │   ├── api-conventions/
    │   │   └── SKILL.md
    │   └── deploy-prod/
    │       └── SKILL.md
    ├── agents/                 # 项目子代理
    │   ├── backend-reviewer.md
    │   └── frontend-reviewer.md
    └── rules/                  # 模块化规则
        ├── code-style.md
        ├── testing.md
        ├── security.md
        └── api-conventions.md
```

---

## 九、`.claude/skills/` — 项目技能文件夹

### 与全局技能的区别

| 特性 | 全局技能 (`~/.claude/skills/`) | 项目技能 (`.claude/skills/`) |
|------|------------------------------|----------------------------|
| 作用范围 | 所有项目 | 当前项目 |
| 共享性 | 个人 | 团队（可提交 Git） |
| 优先级 | 低 | 高 |

### 项目技能示例

```
.claude/skills/
├── api-conventions/
│   └── SKILL.md               # 项目 API 约定
├── deploy-prod/
│   ├── SKILL.md
│   └── scripts/
│       └── deploy.sh
└── legacy-migration/
    └── SKILL.md               # 遗留系统迁移指南
```

---

## 十、`.claude/agents/` — 项目子代理文件夹

### 用途

定义 **项目特定** 的子代理，比如针对特定代码库的审查者。

### 示例

```
.claude/agents/
├── backend-reviewer.md        # 后端代码审查
├── frontend-reviewer.md       # 前端代码审查
├── db-migration-reviewer.md   # 数据库迁移审查
└── api-compliance-checker.md  # API 合规检查
```

---

## 十一、`.claude/rules/` — 模块化规则文件夹

### 作用

将大型项目的指令 **按主题分割** 到多个文件，便于管理。

### 目录结构

```
.claude/rules/
├── _global.md                 # 全局规则（无路径限制）
├── code-style.md              # 代码风格
├── testing.md                 # 测试约定
├── security.md                # 安全要求
├── api-conventions.md         # API 规范
├── frontend/
│   ├── react-rules.md         # React 相关规则
│   └── css-rules.md           # CSS 相关规则
└── backend/
    ├── api-rules.md           # API 相关规则
    └── database-rules.md      # 数据库相关规则
```

### 路径特定规则

使用 YAML frontmatter 限定规则适用范围：

```markdown
---
paths:
  - "src/api/**/*.ts"
  - "src/backend/**/*.ts"
---

# API 开发规则

所有 API 端点必须：
1. 包含输入验证
2. 返回标准错误格式
3. 包含 OpenAPI 文档
```

### Glob 模式示例

| 模式 | 匹配内容 |
|------|---------|
| `**/*.ts` | 所有 TypeScript 文件 |
| `src/api/**` | src/api/ 下所有文件 |
| `**/*.{ts,tsx}` | .ts 和 .tsx 文件 |
| `!**/*.test.ts` | 排除测试文件 |
| `src/**/*.ts` | src/ 下递归所有 .ts 文件 |

---

## 十二、自动创建的隐藏文件夹

### 会话缓存

```
~/.claude/cache/
├── sessions/                 # 会话缓存
├── mcp/                      # MCP 服务器缓存
└── ...
```

### 备份文件夹

Claude Code 会自动创建配置文件的 **时间戳备份**：

```
~/.claude/
├── settings.json
├── settings.json.20250115_143000
├── settings.json.20250114_091500
└── settings.json.20250113_163000
```

保留最近 **5 个备份**，防止数据丢失。

---

## 十三、Monorepo 文件夹处理

### 嵌套 `.claude/` 目录

在 Monorepo 中，子包可以有自己的 `.claude/` 目录：

```
monorepo/
├── .claude/                  # 根目录配置
│   ├── CLAUDE.md             # 整体项目指令
│   └── settings.json
├── packages/
│   ├── frontend/
│   │   ├── .claude/          # 前端专用配置
│   │   │   ├── CLAUDE.md
│   │   │   └── skills/
│   │   └── src/
│   └── backend/
│       ├── .claude/          # 后端专用配置
│       │   ├── CLAUDE.md
│       │   └── skills/
│       └── src/
```

### 规则继承顺序

1. 子包的 `.claude/CLAUDE.md`
2. 父目录的 `.claude/CLAUDE.md`
3. 全局的 `~/.claude/CLAUDE.md`

---

## 十四、文件夹管理最佳实践

### 1. 全局 vs 项目配置

| 配置类型 | 存放位置 | 原因 |
|---------|---------|------|
| 个人偏好 | `~/.claude/` | 所有项目共享 |
| 团队约定 | `.claude/` | 提交 Git，团队一致 |
| 敏感信息 | `.claude/*.local.*` | 不提交，本地使用 |
| 项目特定技能 | `.claude/skills/` | 仅该项目需要 |
| 通用技能 | `~/.claude/skills/` | 所有项目复用 |

### 2. 文件夹清理

```bash
# 查看项目数据大小
du -sh ~/.claude/projects/*

# 清理旧会话（保留最近 30 天）
find ~/.claude/projects/*/sessions -type d -mtime +30 -exec rm -rf {} +

# 清理备份文件（保留最新 3 个）
ls -t ~/.claude/settings.json.* | tail -n +4 | xargs rm -f
```

### 3. .gitignore 配置

```gitignore
# Claude Code 本地配置
.claude/settings.local.json
.claude/CLAUDE.local.md
CLAUDE.local.md
.claude/*.local.*

# 项目数据（自动生成）
.claude/projects/

# MCP 本地配置（含敏感信息）
.mcp.json.local
```

---

## 十五、文件夹速查表

| 文件夹路径 | 作用范围 | 是否提交 Git | 内容 |
|-----------|---------|-------------|------|
| `~/.claude/` | 全局 | 否 | 个人全局配置 |
| `~/.claude/skills/` | 全局 | 否 | 个人技能 |
| `~/.claude/agents/` | 全局 | 否 | 个人子代理 |
| `~/.claude/projects/` | 全局 | 否 | 项目数据和会话 |
| `.claude/` | 项目 | 是 | 项目配置 |
| `.claude/skills/` | 项目 | 是 | 项目技能 |
| `.claude/agents/` | 项目 | 是 | 项目子代理 |
| `.claude/rules/` | 项目 | 是 | 模块化规则 |

---

## 十六、文件夹操作命令

### 在 Claude Code 中

```
> /init                      # 初始化项目 .claude/ 目录
> /memory                    # 打开记忆文件夹选择器
> /skills                    # 管理技能
> /agents                    # 管理子代理
```

### 命令行操作

```bash
# 创建项目配置目录
mkdir -p .claude/skills .claude/agents .claude/rules

# 创建全局技能目录
mkdir -p ~/.claude/skills/my-skill

# 查看项目数据
ls ~/.claude/projects/

# 查看当前会话
ls ~/.claude/projects/$(pwd | md5sum | cut -d' ' -f1)/sessions/
```

---

## 十七、总结

Claude Code 的文件夹结构设计遵循 **分层和模块化** 原则：

| 层级 | 文件夹 | 作用 |
|------|--------|------|
| **系统级** | `/Library/Application Support/` 等 | IT 策略部署 |
| **用户级** | `~/.claude/` | 个人全局配置 |
| **项目级** | `.claude/` | 项目特定配置 |

**核心原则**：
1. **全局配置** 放在 `~/.claude/`，个人使用
2. **团队配置** 放在 `.claude/`，提交 Git 共享
3. **本地覆盖** 用 `*.local.*` 文件，自动 gitignore
4. **模块化** 用 `rules/` 和 `skills/` 分组管理
5. **自动化** 用 `projects/` 自动存储项目数据

掌握这些文件夹结构后，你就能完全掌控 Claude Code 的行为，并根据团队需求灵活配置！

---

> 📚 **参考资料**：[目录结构](https://code.claude.com/docs/en/settings#configuration-scopes) | [内存管理](https://code.claude.com/docs/en/memory) | [Skills](https://code.claude.com/docs/en/skills)
