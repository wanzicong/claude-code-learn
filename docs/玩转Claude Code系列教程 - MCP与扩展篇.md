# 🔌 玩转 Claude Code 系列教程 — MCP 与扩展篇

> **一句话概括**：本篇讲解如何通过 MCP 服务器、Skills、Hooks、子代理和插件扩展 Claude Code 的能力边界，让它连接你的整个工具链。

---

## 一、总述

Claude Code 的内置能力已经很强大，但它真正的威力在于 **可扩展性**。通过以下五大扩展机制，你可以让 Claude Code 连接几乎任何外部工具和服务：

| 扩展机制 | 作用 | 适用场景 |
|---------|------|---------|
| **MCP 服务器** | 连接外部工具和数据源 | 数据库、API、第三方服务 |
| **Skills（技能）** | 添加领域知识和可复用工作流 | 项目约定、自动化流程 |
| **Hooks（钩子）** | 在特定时机自动执行脚本 | 代码检查、格式化、通知 |
| **子代理（Sub-agents）** | 委托专门任务给独立代理 | 代码审查、安全检查、调研 |
| **插件（Plugins）** | 打包分发的扩展集合 | 社区工具、团队工具包 |

---

## 二、MCP 服务器 — 连接外部世界

### 什么是 MCP？

MCP（Model Context Protocol，模型上下文协议）是一个开源标准，用于 AI 与工具的集成。通过 MCP 服务器，Claude Code 可以访问你的工具、数据库和 API。

### MCP 能做什么？

连接 MCP 服务器后，你可以这样使用 Claude Code：

```
> 实现 JIRA 工单 ENG-4521 描述的功能，并在 GitHub 上创建 PR
> 检查 Sentry 和 Statsig，查看 ENG-4521 功能的使用情况
> 根据 PostgreSQL 数据库，找到使用了 ENG-4521 功能的 10 个随机用户的邮箱
> 根据 Slack 中发布的新 Figma 设计更新我们的标准邮件模板
> 创建 Gmail 草稿，邀请这 10 个用户参加新功能的反馈会议
```

### 安装 MCP 服务器

#### 方式一：远程 HTTP 服务器（推荐）

```bash
# 基本语法
claude mcp add --transport http <名称> <URL>

# 示例：连接 Notion
claude mcp add --transport http notion https://mcp.notion.com/mcp

# 带认证头的示例
claude mcp add --transport http secure-api https://api.example.com/mcp \
  --header "Authorization: Bearer your-token"
```

#### 方式二：远程 SSE 服务器

```bash
# 示例：连接 Asana
claude mcp add --transport sse asana https://mcp.asana.com/sse
```

#### 方式三：本地 stdio 服务器

```bash
# 示例：添加 Airtable 服务器
claude mcp add --transport stdio --env AIRTABLE_API_KEY=YOUR_KEY airtable \
  -- npx -y airtable-mcp-server
```

> ⚠️ **Windows 用户注意**：在原生 Windows（非 WSL）上，使用 `npx` 的本地 MCP 服务器需要 `cmd /c` 包装：
> ```bash
> claude mcp add --transport stdio my-server -- cmd /c npx -y @some/package
> ```

### 管理 MCP 服务器

```bash
claude mcp list              # 列出所有已配置的服务器
claude mcp get github        # 获取特定服务器详情
claude mcp remove github     # 移除服务器
```

在 Claude Code 会话中：

```
> /mcp                       # 检查服务器状态和认证
```

### MCP 安装范围

| 范围 | 存储位置 | 说明 |
|------|---------|------|
| **local**（默认） | `~/.claude.json` | 仅你在当前项目可用 |
| **project** | `.mcp.json`（项目根目录） | 团队共享，可提交到 Git |
| **user** | `~/.claude.json` | 你在所有项目可用 |

```bash
# 指定范围
claude mcp add --transport http stripe --scope local https://mcp.stripe.com
claude mcp add --transport http paypal --scope project https://mcp.paypal.com/mcp
claude mcp add --transport http hubspot --scope user https://mcp.hubspot.com/anthropic
```

### 实战示例

#### 示例 1：连接 GitHub 进行代码审查

```bash
# 1. 添加 GitHub MCP 服务器
claude mcp add --transport http github https://api.githubcopilot.com/mcp/

# 2. 在 Claude Code 中认证
> /mcp

# 3. 使用
> 审查 PR #456 并提出改进建议
> 为我们刚发现的 Bug 创建一个新 Issue
> 显示分配给我的所有开放 PR
```

#### 示例 2：查询 PostgreSQL 数据库

```bash
# 1. 添加数据库服务器
claude mcp add --transport stdio db -- npx -y @bytebase/dbhub \
  --dsn "postgresql://readonly:pass@prod.db.com:5432/analytics"

# 2. 自然语言查询
> 这个月的总收入是多少？
> 显示 orders 表的架构
> 找到 90 天内没有购买的客户
```

#### 示例 3：监控 Sentry 错误

```bash
# 1. 添加 Sentry MCP 服务器
claude mcp add --transport http sentry https://mcp.sentry.dev/mcp

# 2. 认证
> /mcp

# 3. 调试生产问题
> 过去 24 小时最常见的错误是什么？
> 显示错误 ID abc123 的堆栈跟踪
> 哪次部署引入了这些新错误？
```

### 使用 MCP 资源

使用 `@` 引用 MCP 资源：

```
> 分析 @github:issue://123 并建议修复方案
> 比较 @postgres:schema://users 和 @docs:file://database/user-model
```

---

## 三、Skills（技能）— 领域知识和工作流

### 什么是 Skills？

Skills 用领域知识和可复用工作流扩展 Claude 的能力。Claude 在会话开始时看到技能描述，但完整内容只在使用时加载。

### 创建 Skill

在 `.claude/skills/` 目录下创建：

```markdown
# .claude/skills/api-conventions/SKILL.md
---
name: api-conventions
description: 我们服务的 REST API 设计约定
---
# API 约定
- URL 路径使用 kebab-case
- JSON 属性使用 camelCase
- 列表端点始终包含分页
- 在 URL 路径中版本化 API (/v1/, /v2/)
```

### 创建可调用的工作流 Skill

```markdown
# .claude/skills/fix-issue/SKILL.md
---
name: fix-issue
description: 修复 GitHub Issue
disable-model-invocation: true
---
分析并修复 GitHub Issue：$ARGUMENTS。

1. 使用 `gh issue view` 获取 Issue 详情
2. 理解 Issue 中描述的问题
3. 搜索代码库中的相关文件
4. 实现必要的更改来修复问题
5. 编写并运行测试验证修复
6. 确保代码通过 lint 和类型检查
7. 创建描述性的提交消息
8. 推送并创建 PR
```

使用方式：

```
> /fix-issue 1234
```

---

## 四、Hooks（钩子）— 自动化保障

### 什么是 Hooks？

Hooks 在 Claude 工作流的特定时机自动运行脚本。与 CLAUDE.md 中的建议性指令不同，Hooks 是 **确定性的**，保证动作一定会发生。

### 常见用途

- 每次文件编辑后运行 ESLint
- 阻止对 migrations 文件夹的写入
- 提交前自动格式化代码
- 工具调用前进行自定义权限验证

### 创建 Hook

让 Claude 帮你写：

```
> 写一个 Hook，在每次文件编辑后运行 eslint
> 写一个 Hook，阻止对 migrations 文件夹的写入
```

或使用交互式配置：

```
> /hooks
```

也可以直接编辑 `.claude/settings.json`。

---

## 五、子代理（Sub-agents）— 专业委托

### 什么是子代理？

子代理在 **独立的上下文** 中运行，有自己的工具集。它们适合需要读取大量文件或需要专注处理的任务，不会污染你的主对话上下文。

### 创建自定义子代理

```markdown
# .claude/agents/security-reviewer.md
---
name: security-reviewer
description: 审查代码中的安全漏洞
tools: Read, Grep, Glob, Bash
model: opus
---
你是一名高级安全工程师。审查代码中的：
- 注入漏洞（SQL、XSS、命令注入）
- 认证和授权缺陷
- 代码中的密钥或凭证
- 不安全的数据处理

提供具体的行号引用和建议修复。
```

### 使用子代理

```
> 使用子代理审查这段代码的安全问题
> 使用子代理调查我们的认证系统如何处理 Token 刷新
```

### 子代理的核心优势

子代理在 **独立的上下文窗口** 中运行，完成后只返回摘要。这意味着：

- 不会消耗你主对话的上下文空间
- 可以进行大量文件读取和探索
- 适合长会话中的调研和验证任务

### 查看和管理子代理

```
> /agents
```

---

## 六、插件（Plugins）— 打包分发

### 什么是插件？

插件将 Skills、Hooks、子代理和 MCP 服务器打包成一个可安装的单元。

### 安装插件

```
> /plugin
```

然后从市场浏览和安装。

### 推荐插件类型

- **代码智能插件**：为你的编程语言提供精确的符号导航和编辑后自动错误检测
- **团队工具包**：打包团队常用的工具和配置
- **社区扩展**：来自社区的各种实用工具

---

## 七、扩展机制选择指南

| 需求 | 推荐机制 | 原因 |
|------|---------|------|
| 连接外部 API/数据库 | **MCP** | 标准化的工具集成协议 |
| 添加项目约定和知识 | **Skills** | 按需加载，不浪费上下文 |
| 每次必须执行的操作 | **Hooks** | 确定性执行，零例外 |
| 需要独立上下文的任务 | **子代理** | 隔离上下文，返回摘要 |
| 打包分发多种扩展 | **插件** | 一键安装，团队一致 |
| 持久化项目指令 | **CLAUDE.md** | 每次会话自动加载 |

---

## 八、总结

Claude Code 的扩展系统让它从一个 AI 编程助手变成了一个 **可连接整个开发工具链的智能平台**：

- **MCP** 让 Claude 能访问数据库、API、Issue 追踪器等外部服务
- **Skills** 让 Claude 掌握你的项目约定和自动化工作流
- **Hooks** 保证关键操作每次都会执行
- **子代理** 在独立上下文中处理专业任务
- **插件** 将所有扩展打包成可分发的单元

掌握这些扩展机制后，Claude Code 的能力将不再受限于内置功能。接下来请阅读 **[自动化与规模化篇]** 学习如何将 Claude Code 集成到 CI/CD 和团队工作流中！

---

> 📚 **参考资料**：[MCP 文档](https://code.claude.com/docs/en/mcp) | [Skills](https://code.claude.com/docs/en/skills) | [Hooks](https://code.claude.com/docs/en/hooks-guide) | [子代理](https://code.claude.com/docs/en/sub-agents)
