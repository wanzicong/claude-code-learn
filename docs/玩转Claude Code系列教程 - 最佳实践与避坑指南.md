# 🎯 玩转 Claude Code 系列教程 — 最佳实践与避坑指南

> **一句话概括**：本篇汇总经过 Anthropic 内部团队和大量开发者验证的最佳实践，以及最常见的失败模式和解决方案，帮你少走弯路。

---

## 一、总述

Claude Code 是一个强大的智能体编程工具，但"强大"也意味着有学习曲线。本篇是系列教程的收官之作，将从两个维度帮你最大化 Claude Code 的效果：

- **最佳实践**：经过验证的高效使用模式
- **避坑指南**：常见失败模式及其解决方案

**核心认知**：大多数最佳实践都基于一个约束——**Claude 的上下文窗口会很快填满，而性能会随着填满而下降**。管理上下文是使用 Claude Code 最重要的技能。

---

## 二、六大最佳实践

### 实践 1：给 Claude 验证自己工作的方式 ⭐⭐⭐

> 这是你能做的 **最高杠杆** 的事情。

Claude 在能够验证自己工作时表现会 **显著提升**——运行测试、对比截图、验证输出。

| 策略 | ❌ 之前 | ✅ 之后 |
|------|--------|--------|
| 提供验证标准 | "实现一个验证邮箱的函数" | "写一个 validateEmail 函数。测试用例：user@example.com 为 true，invalid 为 false，user@.com 为 false。实现后运行测试" |
| 视觉验证 UI | "让仪表板更好看" | "[粘贴截图] 实现这个设计。截图结果并与原图对比。列出差异并修复" |
| 解决根本原因 | "构建失败了" | "构建失败，错误信息：[粘贴错误]。修复它并验证构建成功。解决根本原因，不要压制错误" |

**验证方式清单：**
- ✅ 测试套件
- ✅ Linter 检查
- ✅ 截图对比
- ✅ Bash 命令检查输出
- ✅ Chrome 扩展进行 UI 测试

### 实践 2：先探索，再规划，再编码

让 Claude 直接跳到编码可能会解决错误的问题。推荐的四阶段工作流：

```
阶段 1：探索（计划模式）
┌─────────────────────────────────────────────┐
│ > 读取 /src/auth 并理解我们如何处理会话和登录  │
│ > 同时看看我们如何管理密钥的环境变量           │
└─────────────────────────────────────────────┘
          ↓
阶段 2：规划（计划模式）
┌─────────────────────────────────────────────┐
│ > 我想添加 Google OAuth。哪些文件需要修改？    │
│ > 会话流程是什么？创建一个计划。               │
│ > [按 Ctrl+G 在编辑器中编辑计划]              │
└─────────────────────────────────────────────┘
          ↓
阶段 3：实现（正常模式）
┌─────────────────────────────────────────────┐
│ > 按照你的计划实现 OAuth 流程。为回调处理器     │
│ > 编写测试，运行测试套件并修复任何失败。        │
└─────────────────────────────────────────────┘
          ↓
阶段 4：提交
┌─────────────────────────────────────────────┐
│ > 用描述性消息提交并创建 PR                    │
└─────────────────────────────────────────────┘
```

**何时跳过规划？**
- 范围明确且修改很小（修复拼写错误、添加日志行、重命名变量）
- 你能用一句话描述 diff
- 你对要修改的代码很熟悉

### 实践 3：提供具体的上下文

| 策略 | ❌ 之前 | ✅ 之后 |
|------|--------|--------|
| 限定任务范围 | "给 foo.py 添加测试" | "为 foo.py 编写测试，覆盖用户已登出的边界情况。避免使用 mock。" |
| 指向信息源 | "为什么 ExecutionFactory 的 API 这么奇怪？" | "查看 ExecutionFactory 的 git 历史，总结它的 API 是如何演变的" |
| 引用现有模式 | "添加一个日历组件" | "看看首页现有组件是如何实现的。HotDogWidget.php 是个好例子。按照这个模式实现一个新的日历组件。" |
| 描述症状 | "修复登录 Bug" | "用户报告会话超时后登录失败。检查 src/auth/ 中的认证流程，特别是 token 刷新。写一个复现问题的失败测试，然后修复它" |

### 提供丰富内容的方式

| 方式 | 说明 |
|------|------|
| `@` 引用文件 | `> 解释 @src/auth.js 的逻辑` |
| 粘贴图片 | 复制/粘贴或拖放图片 |
| 提供 URL | 给出文档和 API 参考链接 |
| 管道输入 | `cat error.log \| claude` |
| 让 Claude 自己获取 | "读取 src/config/ 下的所有配置文件" |

### 实践 4：让 Claude 采访你

对于较大的功能，让 Claude 先采访你：

```
我想构建 [简要描述]。用 AskUserQuestion 工具详细采访我。

询问技术实现、UI/UX、边界情况、顾虑和权衡。
不要问显而易见的问题，深入挖掘我可能没考虑到的难点。

持续采访直到我们覆盖了所有内容，然后写一个完整的规格说明到 SPEC.md。
```

规格完成后，**启动一个新会话** 来执行它。新会话有干净的上下文，完全专注于实现。

### 实践 5：积极管理上下文

上下文窗口是你最重要的资源。以下是管理策略：

| 操作 | 命令 | 何时使用 |
|------|------|---------|
| 清除上下文 | `/clear` | 不相关任务之间 |
| 压缩上下文 | `/compact` | 上下文快满时 |
| 定向压缩 | `/compact 聚焦 API 更改` | 保留特定内容 |
| 部分压缩 | `Esc+Esc` → 选择消息 → 从此处总结 | 只压缩部分对话 |
| 查看上下文 | `/context` | 检查什么在占用空间 |
| 使用子代理 | "使用子代理调查 X" | 调研任务 |

**关键规则**：如果你已经纠正 Claude 两次以上同一个问题，上下文已经被失败的尝试污染了。运行 `/clear` 并用更好的初始提示重新开始。

### 实践 6：委托而非指令

把 Claude 当作一个能干的同事来委托任务，给出上下文和方向，然后信任它去处理细节：

```
# ❌ 过度指令
> 打开 src/payments/checkout.js，找到第 45 行的 processPayment 函数，
> 在 try 块中添加一个 if 语句检查 card.expired...

# ✅ 委托
> 结账流程对过期信用卡的用户不工作了。
> 相关代码在 src/payments/ 中。你能调查并修复吗？
```

你不需要指定读哪些文件或运行什么命令。Claude 会自己搞清楚。

---

## 三、五大常见失败模式

### 陷阱 1：厨房水槽会话 🚫

**症状**：你从一个任务开始，然后问了不相关的问题，又回到第一个任务。上下文充满了无关信息。

**解决方案**：

```
> /clear    # 在不相关任务之间清除上下文
```

### 陷阱 2：反复纠正 🚫

**症状**：Claude 做错了，你纠正它，还是错的，你再纠正。上下文被失败的尝试污染。

**解决方案**：

两次纠正失败后，停止纠正。运行 `/clear`，写一个更好的初始提示，把你学到的东西融入进去。

```
# 干净的会话 + 更好的提示 >> 长会话 + 累积的纠正
```

### 陷阱 3：过度膨胀的 CLAUDE.md 🚫

**症状**：CLAUDE.md 太长，Claude 忽略了一半内容，因为重要规则淹没在噪音中。

**解决方案**：

无情地精简。对每一行问自己：**"删除这行会导致 Claude 犯错吗？"** 如果不会，删掉它。

```markdown
# ❌ 膨胀的 CLAUDE.md
- 写干净的代码（废话）
- 使用有意义的变量名（Claude 已经知道）
- 遵循 SOLID 原则（太泛泛）
- [50 行详细的 API 文档]（应该链接而非内联）

# ✅ 精简的 CLAUDE.md
- 测试命令：npm run test:unit（Claude 猜不到）
- 使用 pnpm 而非 npm（与默认不同）
- API 端点必须包含分页（项目特定规则）
```

### 陷阱 4：信任-验证差距 🚫

**症状**：Claude 产出了看起来合理但实际上不处理边界情况的实现。

**解决方案**：

始终提供验证方式（测试、脚本、截图）。**如果你无法验证它，就不要发布它。**

### 陷阱 5：无限探索 🚫

**症状**：你让 Claude "调查"某个东西但没有限定范围。Claude 读了几百个文件，填满了上下文。

**解决方案**：

```
# ❌ 无限范围
> 调查我们的认证系统

# ✅ 限定范围
> 使用子代理调查 src/auth/ 中的 token 刷新逻辑
```

使用子代理进行探索，这样调查不会消耗你的主上下文。

---

## 四、高效提示词模板

### 模板 1：Bug 修复

```
[描述症状]
相关代码在 [目录/文件]。
写一个能复现问题的失败测试，然后修复它。
运行测试验证修复成功。
```

### 模板 2：新功能

```
我想在 [位置] 添加 [功能描述]。
参考 [现有模式/文件] 的实现方式。
要求：[具体要求列表]
实现后运行 [测试命令] 验证。
```

### 模板 3：代码审查

```
审查 [文件/PR] 的更改。
关注：安全问题、性能问题、边界情况、与现有模式的一致性。
对每个问题提供具体的行号和修复建议。
```

### 模板 4：重构

```
将 [文件/模块] 从 [旧模式] 重构为 [新模式]。
保持相同的外部行为。
参考 [示例文件] 的模式。
重构后运行 [测试命令] 确保没有回归。
```

### 模板 5：探索性调研

```
使用子代理调查 [具体问题]。
重点关注 [目录/文件范围]。
总结发现并推荐下一步行动。
```

---

## 五、培养你的直觉

本指南中的模式不是一成不变的。它们是在一般情况下有效的起点，但可能不适用于每种情况。

**需要注意的信号：**

| 信号 | 含义 | 行动 |
|------|------|------|
| Claude 产出了很好的结果 | 注意你做了什么：提示结构、提供的上下文、使用的模式 | 记录并复用 |
| Claude 在挣扎 | 上下文太嘈杂？提示太模糊？任务太大？ | 诊断并调整 |
| 上下文快满了 | 会话太长或探索太多 | `/clear` 或使用子代理 |
| Claude 忽略 CLAUDE.md 规则 | 文件太长，规则被淹没 | 精简 CLAUDE.md |
| 反复纠正无效 | 上下文被污染 | 新会话 + 更好的提示 |

**随着时间推移，你会发展出直觉**：
- 何时具体、何时开放
- 何时规划、何时探索
- 何时清除上下文、何时让它积累
- 何时亲自动手、何时委托给 Claude

---

## 六、速查卡片

### 日常使用速查

```
开始新任务    → cd project && claude
探索代码库    → "给我一个概览" / "解释 @file"
修复 Bug      → "错误信息是 X，修复并验证"
写测试        → "为 X 添加测试，覆盖边界情况"
提交代码      → /commit-push-pr
切换任务      → /clear
恢复工作      → claude --continue / claude --resume
```

### 上下文管理速查

```
检查上下文    → /context
清除上下文    → /clear
压缩上下文    → /compact [焦点]
回退操作      → Esc+Esc 或 /rewind
委托调研      → "使用子代理调查 X"
```

### 配置速查

```
初始化项目    → /init
编辑记忆      → /memory
管理权限      → /permissions
管理 MCP      → /mcp
管理代理      → /agents
诊断问题      → /doctor
```

---

## 七、总结

恭喜你完成了 **玩转 Claude Code** 全部 7 篇系列教程！让我们回顾整个系列的核心要点：

| 篇章 | 核心收获 |
|------|---------|
| **总览篇** | Claude Code 是智能体编程工具，能自主完成完整工作流 |
| **安装与快速入门篇** | 30 秒安装，8 步快速上手 |
| **日常工作流篇** | 8 大核心工作流覆盖日常开发 |
| **高级配置篇** | CLAUDE.md + 权限 + 设置层级 = 完全定制 |
| **MCP 与扩展篇** | 5 大扩展机制连接整个工具链 |
| **自动化与规模化篇** | 无头模式 + 并行会话 + CI/CD = 团队级生产力 |
| **最佳实践与避坑指南** | 6 大实践 + 5 大陷阱 = 少走弯路 |

### 最终建议

1. **从小处开始**：先用 Claude Code 完成简单任务，逐步增加复杂度
2. **投资验证**：让 Claude 能验证自己的工作是最高杠杆的投入
3. **管理上下文**：这是使用 Claude Code 最重要的技能
4. **持续迭代**：CLAUDE.md、Skills、Hooks 都需要随项目演进而更新
5. **培养直觉**：注意什么有效、什么无效，发展你自己的使用模式

**现在，打开终端，输入 `claude`，开始你的 AI 编程之旅吧！** 🚀

---

> 📚 **参考资料**：[最佳实践](https://code.claude.com/docs/en/best-practices) | [故障排除](https://code.claude.com/docs/en/troubleshooting) | [完整文档](https://code.claude.com/docs/en/overview)
