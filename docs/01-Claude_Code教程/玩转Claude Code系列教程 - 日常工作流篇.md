# 💼 玩转 Claude Code 系列教程 — 日常工作流篇

> **一句话概括**：本篇覆盖日常开发中最常用的工作流：探索代码库、修 Bug、重构、写测试、创建 PR、处理文档和图片。

---

## 一、总述

掌握了安装和基础操作后，本篇将教你如何在 **实际开发场景** 中高效使用 Claude Code。每个工作流都包含具体的提示词示例，你可以直接复制使用。

本篇涵盖 8 大核心工作流：

1. 理解新代码库
2. 高效修复 Bug
3. 重构代码
4. 编写测试
5. 创建 Pull Request
6. 处理文档
7. 使用图片
8. 使用计划模式

---

## 二、工作流详解

### 工作流 1：理解新代码库

当你刚加入一个新项目，需要快速了解其结构时：

**第一步：获取高层概览**

```bash
cd /path/to/project
claude
```

```
> 给我一个这个代码库的概览
```

**第二步：深入了解具体组件**

```
> 解释这里使用的主要架构模式
> 关键的数据模型有哪些？
> 认证是怎么处理的？
```

**第三步：查找相关代码**

```
> 找到处理用户认证的文件
> 这些认证文件是如何协同工作的？
> 追踪从前端到数据库的登录流程
```

**实用技巧：**
- 从宽泛的问题开始，逐步缩小到具体领域
- 使用项目中的领域术语
- 用 `@` 引用具体文件：`> 解释 @src/utils/auth.js 中的逻辑`

### 工作流 2：高效修复 Bug

**第一步：分享错误信息**

```
> 我运行 npm test 时看到一个错误
```

或者更具体地：

```
> 用户报告登录在会话超时后失败。检查 src/auth/ 中的认证流程，
> 特别是 token 刷新部分。写一个能复现问题的失败测试，然后修复它
```

**第二步：获取修复建议**

```
> 建议几种修复 user.ts 中 @ts-ignore 的方法
```

**第三步：应用修复**

```
> 按照你建议的方式更新 user.ts，添加空值检查
```

**实用技巧：**
- 告诉 Claude 复现问题的命令和堆栈跟踪
- 说明错误是间歇性的还是持续性的
- 让 Claude 解决根本原因，而不是压制错误

### 工作流 3：重构代码

**第一步：识别需要重构的代码**

```
> 找到代码库中已弃用的 API 用法
```

**第二步：获取重构建议**

```
> 建议如何将 utils.js 重构为使用现代 JavaScript 特性
```

**第三步：安全地应用更改**

```
> 将 utils.js 重构为使用 ES2024 特性，同时保持相同的行为
```

**第四步：验证重构**

```
> 运行重构后代码的测试
```

**实用技巧：**
- 让 Claude 解释现代方法的好处
- 在需要时要求保持向后兼容性
- 以小的、可测试的增量进行重构

### 工作流 4：编写测试

**第一步：识别未测试的代码**

```
> 找到 NotificationsService.swift 中没有被测试覆盖的函数
```

**第二步：生成测试脚手架**

```
> 为通知服务添加测试
```

**第三步：添加有意义的测试用例**

```
> 为通知服务的边界条件添加测试用例
```

**第四步：运行并验证测试**

```
> 运行新测试并修复任何失败
```

**实用技巧：**
- Claude 会检查你现有的测试文件，匹配已有的风格、框架和断言模式
- 要求 Claude 识别你可能遗漏的边界情况
- 具体说明你想验证什么行为

### 工作流 5：创建 Pull Request

**快速方式：**

```
> /commit-push-pr
```

这会一步完成提交、推送和创建 PR。

**分步方式：**

```
> 总结我对认证模块所做的更改
> 创建一个 PR
> 用更多关于安全改进的上下文增强 PR 描述
```

**实用技巧：**
- 提交前审查 Claude 生成的 PR 描述
- 让 Claude 突出潜在风险或注意事项
- 使用 `gh pr create` 创建 PR 后，会话会自动关联到该 PR

### 工作流 6：处理文档

**第一步：识别未文档化的代码**

```
> 找到 auth 模块中没有 JSDoc 注释的函数
```

**第二步：生成文档**

```
> 为 auth.js 中未文档化的函数添加 JSDoc 注释
```

**第三步：审查和增强**

```
> 用更多上下文和示例改进生成的文档
```

**实用技巧：**
- 指定你想要的文档风格（JSDoc、docstrings 等）
- 要求在文档中包含示例
- 请求为公共 API、接口和复杂逻辑编写文档

### 工作流 7：使用图片

Claude Code 支持图片分析，你可以通过以下方式添加图片：

1. **拖放**：将图片拖入 Claude Code 窗口
2. **粘贴**：复制图片后用 `Ctrl+V` 粘贴
3. **路径引用**：`> 分析这张图片：/path/to/image.png`

**使用场景：**

```
> 这是错误的截图，是什么导致的？
> 这是我们当前的数据库架构图，我们应该如何为新功能修改它？
> 生成匹配这个设计稿的 CSS
> 这个截图中有什么 UI 元素？
```

### 工作流 8：使用计划模式（Plan Mode）

计划模式让 Claude 只进行只读分析，非常适合在修改代码前先探索和规划。

**何时使用计划模式：**
- 多步骤实现：功能需要编辑多个文件
- 代码探索：在修改前彻底研究代码库
- 交互式开发：想与 Claude 迭代讨论方向

**如何进入计划模式：**

```bash
# 方式一：启动时指定
claude --permission-mode plan

# 方式二：会话中切换
# 按 Shift+Tab 循环切换权限模式
```

**示例：规划复杂重构**

```bash
claude --permission-mode plan
```

```
> 我需要将认证系统重构为使用 OAuth2。创建一个详细的迁移计划。
```

Claude 会分析当前实现并创建全面的计划。然后你可以用后续问题细化：

```
> 向后兼容性怎么处理？
> 数据库迁移应该怎么做？
```

按 `Ctrl+G` 可以在文本编辑器中打开计划进行直接编辑。

---

## 三、文件和目录引用

使用 `@` 快速引用文件或目录：

```
> 解释 @src/utils/auth.js 中的逻辑          # 引用单个文件
> @src/components 的结构是什么？              # 引用目录
> @file1.js 和 @file2.js 有什么区别？        # 引用多个文件
```

---

## 四、恢复和管理会话

### 恢复之前的对话

```bash
claude --continue          # 继续最近的对话
claude --resume            # 打开会话选择器
claude --from-pr 123       # 恢复关联到特定 PR 的会话
```

### 命名会话

```
> /rename auth-refactor    # 给当前会话命名
```

```bash
claude --resume auth-refactor   # 按名称恢复
```

### 会话选择器快捷键

| 快捷键 | 操作 |
|--------|------|
| `↑` / `↓` | 在会话间导航 |
| `→` / `←` | 展开/折叠分组会话 |
| `Enter` | 选择并恢复会话 |
| `P` | 预览会话内容 |
| `R` | 重命名会话 |
| `/` | 搜索过滤会话 |
| `A` | 切换当前目录/所有项目 |
| `B` | 按当前 Git 分支过滤 |
| `Esc` | 退出选择器 |

---

## 五、总结

本篇介绍了 Claude Code 在日常开发中的 8 大核心工作流：

| 工作流 | 核心提示词 |
|--------|-----------|
| 理解代码库 | `给我一个这个代码库的概览` |
| 修复 Bug | `我运行 X 时看到错误 Y，帮我修复` |
| 重构代码 | `将 X 重构为使用 Y 特性` |
| 编写测试 | `为 X 添加测试，覆盖边界条件` |
| 创建 PR | `/commit-push-pr` |
| 处理文档 | `为 X 中未文档化的函数添加注释` |
| 使用图片 | 拖放/粘贴/路径引用 |
| 计划模式 | `Shift+Tab` 切换 |

掌握这些工作流后，你的日常开发效率将大幅提升。接下来请阅读 **[高级配置篇]** 学习如何深度定制 Claude Code！

---

> 📚 **参考资料**：[常见工作流](https://code.claude.com/docs/en/common-workflows) | [交互模式](https://code.claude.com/docs/en/interactive-mode)
