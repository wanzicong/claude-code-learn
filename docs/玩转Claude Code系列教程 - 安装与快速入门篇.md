# 🛠️ 玩转 Claude Code 系列教程 — 安装与快速入门篇

> **一句话概括**：本篇手把手教你安装 Claude Code、完成登录，并通过 8 个步骤快速上手，5 分钟内开始用 AI 编程。

---

## 一、总述

本篇是系列教程的第二篇，目标是让你在 **5 分钟内** 完成从安装到第一次使用 Claude Code 的全过程。你将学会：

- 如何在不同操作系统上安装 Claude Code
- 如何登录和认证
- 如何启动第一个会话
- 基础命令和快捷键
- 常用的 CLI 命令参考

---

## 二、安装 Claude Code

### 方式一：原生安装（推荐 ⭐）

原生安装会自动在后台更新，始终保持最新版本。

#### macOS / Linux / WSL

```bash
curl -fsSL https://claude.ai/install.sh | bash
```

#### Windows PowerShell

```powershell
irm https://claude.ai/install.ps1 | iex
```

#### Windows CMD

```batch
curl -fsSL https://claude.ai/install.cmd -o install.cmd && install.cmd && del install.cmd
```

### 方式二：Homebrew（macOS）

```bash
brew install --cask claude-code
```

> ⚠️ Homebrew 安装不会自动更新，需要定期运行 `brew upgrade claude-code`

### 方式三：WinGet（Windows）

```powershell
winget install Anthropic.ClaudeCode
```

> ⚠️ WinGet 安装不会自动更新，需要定期运行 `winget upgrade Anthropic.ClaudeCode`

### 验证安装

```bash
claude --version
```

如果看到版本号输出，说明安装成功！

---

## 三、登录与认证

### 第一次登录

启动 Claude Code 后会自动提示登录：

```bash
claude
# 首次使用会提示你登录
```

或者在会话中使用命令：

```bash
/login
# 按照提示完成登录
```

### 支持的账户类型

| 账户类型 | 说明 |
|---------|------|
| **Claude Pro/Max/Teams/Enterprise** | 推荐，通过 claude.com 订阅 |
| **Claude Console** | API 访问，需要预充值额度 |
| **Amazon Bedrock** | 企业云服务商 |
| **Google Vertex AI** | 企业云服务商 |
| **Microsoft Foundry** | 企业云服务商 |

登录成功后，凭证会被安全存储，后续无需重复登录。如需切换账户，使用 `/login` 命令。

---

## 四、快速入门 8 步走

### 步骤 1：启动第一个会话

```bash
cd /path/to/your/project   # 进入你的项目目录
claude                       # 启动 Claude Code
```

你会看到 Claude Code 的欢迎界面，包含会话信息、最近对话和最新更新。

### 步骤 2：问第一个问题

```
> 这个项目是做什么的？
```

Claude 会分析你的文件并提供摘要。你还可以问更具体的问题：

```
> 这个项目使用了哪些技术？
> 主入口文件在哪里？
> 解释一下文件夹结构
```

> 💡 Claude Code 会按需读取你的文件，你不需要手动添加上下文。

### 步骤 3：做第一次代码修改

```
> 在主文件中添加一个 hello world 函数
```

Claude Code 会：
1. ✅ 找到合适的文件
2. ✅ 展示提议的修改
3. ✅ 请求你的批准
4. ✅ 执行编辑

> 🔒 Claude Code 在修改文件前总是会请求许可。你可以逐个批准，也可以启用"全部接受"模式。

### 步骤 4：使用 Git

```
> 我修改了哪些文件？
> 用描述性的消息提交我的更改
> 创建一个名为 feature/quickstart 的新分支
> 显示最近 5 次提交
```

### 步骤 5：修复 Bug

```
> 用户可以提交空表单，这是一个 Bug，帮我修复
```

Claude Code 会：
- 定位相关代码
- 理解上下文
- 实现修复方案
- 运行测试（如果有的话）

### 步骤 6：重构代码

```
> 将认证模块从回调方式重构为 async/await
```

### 步骤 7：编写测试

```
> 为计算器函数编写单元测试
```

### 步骤 8：代码审查

```
> 审查我的更改并提出改进建议
```

---

## 五、必备命令速查表

### CLI 启动命令

| 命令 | 功能 | 示例 |
|------|------|------|
| `claude` | 启动交互模式 | `claude` |
| `claude "任务"` | 带初始提示启动 | `claude "修复构建错误"` |
| `claude -p "查询"` | 单次查询后退出 | `claude -p "解释这个函数"` |
| `claude -c` | 继续最近的对话 | `claude -c` |
| `claude -r` | 恢复之前的对话 | `claude -r` |
| `claude commit` | 创建 Git 提交 | `claude commit` |
| `claude update` | 更新到最新版本 | `claude update` |

### 会话内命令

| 命令 | 功能 |
|------|------|
| `/help` | 显示可用命令 |
| `/clear` | 清除对话历史 |
| `/compact` | 压缩上下文 |
| `/model` | 切换模型 |
| `/resume` | 恢复之前的会话 |
| `/rename` | 重命名当前会话 |
| `/memory` | 编辑记忆文件 |
| `/init` | 初始化 CLAUDE.md |
| `/permissions` | 管理权限 |
| `/mcp` | 管理 MCP 服务器 |
| `/agents` | 管理子代理 |
| `/context` | 查看上下文使用情况 |
| `/doctor` | 诊断常见问题 |
| `exit` 或 `Ctrl+C` | 退出 Claude Code |

### 快捷键

| 快捷键 | 功能 |
|--------|------|
| `Esc` | 停止 Claude 当前操作 |
| `Esc + Esc` | 打开回退菜单 |
| `Shift+Tab` | 切换权限模式（默认 → 自动接受 → 计划模式） |
| `Ctrl+O` | 切换详细模式（查看思考过程） |
| `Ctrl+G` | 在编辑器中打开计划 |
| `Option+T` / `Alt+T` | 切换思考模式开关 |
| `Tab` | 命令补全 |
| `↑` | 命令历史 |
| `?` | 查看所有快捷键 |
| `/` | 查看所有命令和技能 |

---

## 六、常用 CLI 标志

| 标志 | 说明 | 示例 |
|------|------|------|
| `--model` | 指定模型 | `claude --model opus` |
| `--add-dir` | 添加额外工作目录 | `claude --add-dir ../lib` |
| `--permission-mode` | 设置权限模式 | `claude --permission-mode plan` |
| `--output-format` | 输出格式（text/json/stream-json） | `claude -p "查询" --output-format json` |
| `--verbose` | 详细日志 | `claude --verbose` |
| `--continue` | 继续最近对话 | `claude --continue` |
| `--resume` | 恢复指定会话 | `claude --resume auth-refactor` |
| `--fork-session` | 分叉会话 | `claude --continue --fork-session` |
| `--max-turns` | 限制智能体轮次 | `claude -p --max-turns 3 "查询"` |
| `--dangerously-skip-permissions` | 跳过所有权限检查 | 仅在沙箱环境使用！ |

---

## 七、新手小贴士

### 💡 提示要具体

```
# ❌ 模糊的提示
> 修复那个 Bug

# ✅ 具体的提示
> 修复登录 Bug：用户输入错误密码后看到空白屏幕
```

### 💡 使用分步指令

```
> 1. 创建一个新的用户资料数据库表
> 2. 创建获取和更新用户资料的 API 端点
> 3. 构建一个让用户查看和编辑信息的页面
```

### 💡 先让 Claude 探索

```
> 分析数据库架构
```

然后再要求实现：

```
> 构建一个仪表板，显示英国客户最常退货的产品
```

### 💡 善用快捷键

- 按 `?` 查看所有可用快捷键
- 用 `Tab` 进行命令补全
- 按 `↑` 查看命令历史
- 输入 `/` 查看所有命令和技能

---

## 八、总结

恭喜你完成了 Claude Code 的安装和快速入门！现在你已经掌握了：

- ✅ 三种安装方式（原生安装、Homebrew、WinGet）
- ✅ 登录和认证流程
- ✅ 基本的对话和代码修改操作
- ✅ 常用命令和快捷键
- ✅ 新手使用技巧

接下来，请阅读 **[日常工作流篇]** 学习如何在实际开发中高效使用 Claude Code！

---

> 📚 **参考资料**：[Claude Code 快速入门](https://code.claude.com/docs/en/quickstart) | [CLI 参考](https://code.claude.com/docs/en/cli-reference)
